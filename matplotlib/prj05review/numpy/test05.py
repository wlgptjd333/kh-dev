import numpy as np

# sort
# argsort
# argmax
# where(조건, 참값, 거짓값)

# x = np.array([1, 2, 3, 4, 5, 6])
# result = np.where( np.isin(x, 3), "굿", "배드")
# print(result)

rng = np.random.default_rng()
# result = rng.integers(1,46,size=6)
# result = rng.normal(60, 3/3)
print(result)

# @ : 행렬 곱

# np.save, np.load
